# Redis connection settings
REDIS_HOST=localhost
REDIS_PORT=6379

# Authentication (if enabled in redis.conf)
REDIS_PASSWORD=@Theforce@92

# Optional: Redis ACL user (Redis ≥6.0)
REDIS_USER=simongemmill

# Flask session config (if using Redis for session storage)
SESSION_TYPE=redis
SESSION_REDIS_URL=redis://dashboard_user:@Theforce@92@localhost:6379/0

# Optional: Redis DB index
REDIS_DB=0